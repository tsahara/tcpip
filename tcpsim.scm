(define-class <tcp> ()
  ())

(define (tcp-open-to peer)
  )

(define-class <tcp-segment> ()
  ())

(define-method (on-receive segment)

  )

(define (timer-event-add sec thunk)
  (event-)

(let ((a (make <tcp>))
      (b (make <tcp>)))
  (tcp-open-to b)
  (event-loop))
